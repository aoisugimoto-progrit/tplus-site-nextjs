# 実装漏れ機能リスト

## 🔴 完全に実装されていない機能

### 1. **insightsセクション（19の隠れたインサイト）** - 最重要
- ❌ マトリックス図解（CSS Grid複雑レイアウト）
- ❌ 19個の成長戦略リスト
- ❌ マトリックスクリック→詳細スクロール機能
- ❌ マトリックスホバー時のツールチップ

### 2. **用語集ツールチップ機能**
- ❌ `.glossary-term`クラスへのホバー時ツールチップ表示
- ❌ 用語の定義（`data-definition`属性から）
- ❌ 位置計算（画面端対応）
- 📝 実装箇所: 全セクションの専門用語

### 3. **読破率ポップアップ**
- ❌ スクロール進捗に応じたポップアップ表示
- ❌ 20%, 40%, 60%, 80%, 100%のマイルストーン
- ❌ 「読了お疲れ様でした！」メッセージ
- ❌ アニメーション付きポップアップ

### 4. **映画ギャラリー**
- ❌ 映画画像5枚の表示
- ❌ 画像ギャラリーレイアウト
- 📂 画像: `images/movie1.png ~ movie5.png`

### 5. **理解度チェックテスト（quiz）**
- ⚠️ 簡易実装のみ（ボタンだけ）
- ❌ 小テスト①のPDFリンク
- ❌ 小テスト②のPDFリンク
- ❌ テストカードデザイン
- 📂 PDF: `quiz1.pdf`, `quiz2.pdf`

### 6. **サイドバーの詳細機能**
- ⚠️ 基本実装あり
- ❌ リサイズ機能（ドラッグで幅変更）
- ❌ サブメニューの開閉アニメーション
- ❌ 読了マーク（✓）の自動追加

### 7. **アコーディオンの詳細機能**
- ⚠️ 基本実装あり
- ❌ 「事例を見る」ボタン機能
- ❌ 事例コンテンツの展開/折りたたみ
- 📝 実装箇所: `<strong>例：</strong>`や`<strong>具体例：</strong>`を含むp要素

### 8. **コンテンツ欠落**
- ❌ screeningセクションの詳細内容（約487行分）
- ❌ internalセクションの詳細内容（約298行分）
- ❌ event-drivenセクションの詳細内容（約124行分）
- ❌ 参考書籍セクション（book-grid）
- ❌ カードグリッドの完全な内容

---

## 🟡 部分的に実装されているが不完全な機能

### 1. **Accordion コンポーネント**
- ✅ 基本的な開閉機能
- ✅ WAI-ARIA対応
- ❌ アイコンの回転アニメーション（▼ ⇔ ▶）が正常動作していない
- ❌ `maxHeight`の計算が不正確（コンテンツが見切れる）

### 2. **Sidebar コンポーネント**
- ✅ 基本的な目次表示
- ✅ スクロール連動
- ❌ サイドバーの開閉状態が親コンポーネントに反映されない
- ❌ メインコンテンツのマージンが動的に変わらない

### 3. **デザインの崩れ**
- ❌ レスポンシブデザインが不完全（モバイル未対応）
- ❌ カラースキームが元と異なる
- ❌ フォントサイズが統一されていない
- ❌ スペーシングが元と異なる

---

## 📋 実装優先順位

### 🔥 最優先（P0）
1. **Accordionの見切れ問題修正** - ユーザビリティに直結
2. **insightsセクション実装** - 重要コンテンツ
3. **サイドバーとメインコンテンツの連動修正**

### 🟡 優先（P1）
4. **用語集ツールチップ** - 学習体験向上
5. **screeningセクションの完全実装**
6. **internalセクションの完全実装**
7. **event-drivenセクションの完全実装**

### 🟢 通常（P2）
8. **読破率ポップアップ**
9. **映画ギャラリー**
10. **理解度チェックテスト**
11. **サイドバーリサイズ機能**
12. **「事例を見る」ボタン**

---

## 🎨 デザイン修正必要箇所

### 1. **グローバルスタイル**
```css
/* 元のCSS変数が使われていない */
--color-primary: #2D5F4C;  /* 実際にはTailwindの色が使われている */
```

### 2. **Accordionの見切れ**
```tsx
// 現在の実装（問題あり）
style={{
  maxHeight: isOpen ? `${contentRef.current?.scrollHeight}px` : '0px',
  padding: isOpen ? '35px' : '0 35px'
}}

// 問題: scrollHeightが正確に取得できていない
// paddingを含めた高さが計算されていない
```

### 3. **Sidebarのマージン**
```tsx
// 現在の実装（効いていない）
<div className={`transition-all duration-300 ${isOpen ? 'ml-[260px]' : 'ml-0'}`}>
  {/* この部分は親コンポーネントで処理 */}
</div>

// 問題: 実際にはこのdivが使われていない
```

---

## 🔧 修正方針

### Phase 1: 緊急修正（30分）
1. Accordionの`maxHeight`計算を修正
2. Sidebarの開閉とメインコンテンツのマージン連動
3. 基本的なレスポンシブ対応

### Phase 2: コンテンツ追加（1時間）
4. insightsセクション実装（マトリックス + リスト）
5. screening/internal/event-drivenセクションの完全実装
6. quiz/moviesセクションの実装

### Phase 3: 高度な機能（1時間）
7. 用語集ツールチップ
8. 読破率ポップアップ
9. サイドバーリサイズ
10. 「事例を見る」ボタン

---

## 📊 実装進捗

| 機能 | 元のHTML | Next.js | 進捗率 |
|------|----------|---------|--------|
| ヘッダー | ✅ | ✅ | 100% |
| サイドバー | ✅ | ⚠️ | 70% |
| プログレスバー | ✅ | ✅ | 100% |
| アコーディオン | ✅ | ⚠️ | 60% |
| basicsセクション | ✅ | ⚠️ | 80% |
| flowセクション | ✅ | ⚠️ | 80% |
| externalセクション | ✅ | ⚠️ | 70% |
| screeningセクション | ✅ | ❌ | 20% |
| internalセクション | ✅ | ❌ | 20% |
| event-drivenセクション | ✅ | ❌ | 30% |
| **insightsセクション** | ✅ | ❌ | **0%** |
| quizセクション | ✅ | ⚠️ | 30% |
| moviesセクション | ✅ | ⚠️ | 30% |
| 用語集ツールチップ | ✅ | ❌ | 0% |
| 読破率ポップアップ | ✅ | ❌ | 0% |
| サイドバーリサイズ | ✅ | ❌ | 0% |
| 「事例を見る」ボタン | ✅ | ❌ | 0% |

**全体進捗**: 約45%

---

## 🚨 クリティカルな問題

### 1. **コンテンツの見切れ**
- Accordionの高さ計算が不正確
- スクロールしても全文が読めない
- **影響**: ユーザーが教育コンテンツを読めない

### 2. **サイドバーとメインコンテンツの非連動**
- サイドバーを閉じてもメインコンテンツが広がらない
- **影響**: 画面スペースの無駄

### 3. **重要コンテンツの欠落**
- insightsセクション（19のインサイト）が完全に欠落
- **影響**: 教育コンテンツの核心部分が見られない

---

**作成日**: 2026-04-24  
**調査対象**: `/Users/sugimotoaoi/Desktop/tplus-site/education.html` (全1655行)  
**Next.js版**: `/Users/sugimotoaoi/Desktop/tplus-site-nextjs/`
